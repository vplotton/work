
/*
strsplit sinon, parce que la je me fais chier pour rien ...
*/

/*
void    ft_get_team_name(t_env  *env, char  *buff, int  nb, t_interface *itf)
{
    int     team;
    int     i;
    char    *name;

    team =
    i = 0;
    itf->name = (char**)ft_memalloc(nb * sizeof(char*);
    while( i < nb)
    {

        itf->name[i] = ft_strdup(name);
    }

}
*/
